# @PROJECT_NAME@

Retro2DFramework game project.

## Build

```powershell
.\build.bat debug
.\build.bat release
.\build.bat dist
```

The final packaged folder is generated in `build/dist/Release/@PROJECT_TARGET@`.

